# Ignore environment variables
.env

# Ignore dependency lock file (if you want to avoid tracking exact dependencies)
package-lock.json

# Ignore Node modules (if not already included)
node_modules/

# Ignore build and log files (optional but recommended)
dist/
.DS_Store
*.log
